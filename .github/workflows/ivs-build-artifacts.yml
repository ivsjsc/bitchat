name: IVS Build Artifacts

on:
  workflow_dispatch:
  push:
    branches:
      - develop

permissions:
  contents: read

jobs:
  build-artifacts:
    name: Build unsigned release artifacts
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Show toolchain
        run: |
          xcodebuild -version
          swift --version

      - name: Build iOS Simulator release
        run: |
          set -euo pipefail
          xcodebuild \
            -project bitchat.xcodeproj \
            -scheme "bitchat (iOS)" \
            -configuration Release \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath .DerivedData/ios \
            ARCHS='arm64 x86_64' \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

          IOS_APP=$(find .DerivedData/ios/Build/Products -type d -name '*.app' | head -1)
          test -n "$IOS_APP"
          ditto -c -k --sequesterRsrc --keepParent "$IOS_APP" bitchat-iOS-Simulator-Release.zip

      - name: Build macOS universal release
        run: |
          set -euo pipefail
          xcodebuild \
            -project bitchat.xcodeproj \
            -scheme "bitchat (macOS)" \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            -derivedDataPath .DerivedData/macos \
            ARCHS='arm64 x86_64' \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

          MAC_APP=$(find .DerivedData/macos/Build/Products -type d -name '*.app' | head -1)
          test -n "$MAC_APP"
          ditto -c -k --sequesterRsrc --keepParent "$MAC_APP" bitchat-macOS-Universal-Release.zip

      - name: Upload iOS Simulator artifact
        uses: actions/upload-artifact@v4
        with:
          name: bitchat-iOS-Simulator-Release
          path: bitchat-iOS-Simulator-Release.zip
          if-no-files-found: error

      - name: Upload macOS universal artifact
        uses: actions/upload-artifact@v4
        with:
          name: bitchat-macOS-Universal-Release
          path: bitchat-macOS-Universal-Release.zip
          if-no-files-found: error
