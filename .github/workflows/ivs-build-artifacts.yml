name: IVS Build Artifacts

on:
  workflow_dispatch:
  push:
    branches:
      - develop
      - ivs/branding-signing

permissions:
  contents: read

concurrency:
  group: ivs-build-artifacts-${{ github.ref }}
  cancel-in-progress: true

env:
  EXPECTED_BUNDLE_ID: com.ivsjsc.bitchat
  EXPECTED_DISPLAY_NAME: Bitchat IVS

jobs:
  build-artifacts:
    name: Build unsigned IVS release artifacts
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Show toolchain
        run: |
          xcodebuild -version
          swift --version

      - name: Build iOS Simulator release
        run: |
          set -euo pipefail
          xcodebuild \
            -project bitchat.xcodeproj \
            -scheme "bitchat (iOS)" \
            -configuration Release \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath .DerivedData/ios \
            ARCHS='arm64 x86_64' \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

          IOS_APP=$(find .DerivedData/ios/Build/Products -type d -name '*.app' | head -1)
          test -n "$IOS_APP"
          IOS_BUNDLE_ID=$(/usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' "$IOS_APP/Info.plist")
          IOS_DISPLAY_NAME=$(/usr/libexec/PlistBuddy -c 'Print :CFBundleDisplayName' "$IOS_APP/Info.plist")
          echo "iOS bundle: $IOS_BUNDLE_ID"
          echo "iOS display name: $IOS_DISPLAY_NAME"
          test "$IOS_BUNDLE_ID" = "$EXPECTED_BUNDLE_ID"
          test "$IOS_DISPLAY_NAME" = "$EXPECTED_DISPLAY_NAME"
          ditto -c -k --sequesterRsrc --keepParent "$IOS_APP" Bitchat-IVS-iOS-Simulator-Release.zip

      - name: Build macOS universal release
        run: |
          set -euo pipefail
          xcodebuild \
            -project bitchat.xcodeproj \
            -scheme "bitchat (macOS)" \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            -derivedDataPath .DerivedData/macos \
            ARCHS='arm64 x86_64' \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

          MAC_APP=$(find .DerivedData/macos/Build/Products -type d -name '*.app' | head -1)
          test -n "$MAC_APP"
          MAC_BUNDLE_ID=$(/usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' "$MAC_APP/Contents/Info.plist")
          MAC_DISPLAY_NAME=$(/usr/libexec/PlistBuddy -c 'Print :CFBundleDisplayName' "$MAC_APP/Contents/Info.plist")
          echo "macOS bundle: $MAC_BUNDLE_ID"
          echo "macOS display name: $MAC_DISPLAY_NAME"
          test "$MAC_BUNDLE_ID" = "$EXPECTED_BUNDLE_ID"
          test "$MAC_DISPLAY_NAME" = "$EXPECTED_DISPLAY_NAME"
          ditto -c -k --sequesterRsrc --keepParent "$MAC_APP" Bitchat-IVS-macOS-Universal-Release.zip

      - name: Upload iOS Simulator artifact
        uses: actions/upload-artifact@v4
        with:
          name: Bitchat-IVS-iOS-Simulator-Release
          path: Bitchat-IVS-iOS-Simulator-Release.zip
          if-no-files-found: error

      - name: Upload macOS universal artifact
        uses: actions/upload-artifact@v4
        with:
          name: Bitchat-IVS-macOS-Universal-Release
          path: Bitchat-IVS-macOS-Universal-Release.zip
          if-no-files-found: error
